# Trace Guide (dev)

See `TRACE-GUIDE.md` at repo root for full details.

Key changes:
- FIFO-first reconcile actions (`port_materialized`, `transport_attached`).
- Heal actions (`port_rematerialized`, `restart_transport`, `restart_process`).
